import Utility from './Utility';

function ImportTransactions({ currentTheme }) {
  return <Utility currentTheme={currentTheme} defaultSection="import" hideSelector={true} />;
}

export default ImportTransactions;

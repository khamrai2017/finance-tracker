import Utility from './Utility';

function MatchTransactions({ currentTheme }) {
  return <Utility currentTheme={currentTheme} defaultSection="match" hideSelector={true} />;
}

export default MatchTransactions;
